# Web
Flask==3.0.3
gunicorn==22.0.0
Flask-Cors==4.0.1

# ORM / DB
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.32

# Postgres driver (Python 3.13 compatible, wheels available)
psycopg[binary]==3.2.10

# Optional env loader (if you use a .env locally)
python-dotenv==1.0.1
